const hello_or_goodbye = @import("hello-or-goodbye");
const my_package = @import("my-package");

pub fn main() !void {
    // uncomment and ask for completions at locations marked with the pipe symbol:
    // hello.|
    // hello_or_goodbye.|
}
